# Vibe Kanban Environment Variables (Root/.env)
# ================================================
# This file contains BACKEND build-time environment variables.
# Copy this file to `.env` and configure as needed.
#
# Quick setup: cp .env.example .env
#
# NOTE: All variables are OPTIONAL for basic local development!
# See API_KEY_REQUIREMENTS.md for detailed documentation.

# ================================================
# TASK SHARING FEATURES (Optional but recommended)
# ================================================
# Enable task sharing with team members via Clerk authentication
# Requires deploying the remote service or using hosted version

# Clerk issuer URL - Get from your Clerk application settings
# Example: https://your-tenant.clerk.accounts.dev
CLERK_ISSUER=

# Remote API base URL for task sharing
# Example: http://localhost:8081 or https://your-remote-api.com
VK_SHARED_API_BASE=

# ================================================
# ANALYTICS (Optional)
# ================================================
# PostHog analytics - leave empty to disable analytics

# PostHog API key
POSTHOG_API_KEY=

# PostHog API endpoint
POSTHOG_API_ENDPOINT=

# ================================================
# DEVELOPMENT RUNTIME VARIABLES (Optional)
# ================================================
# These are read at runtime, not build time

# Backend server port (0 = auto-assign)
# BACKEND_PORT=0

# Frontend dev server port
# FRONTEND_PORT=3000

# Backend server host
# HOST=127.0.0.1

# Disable git worktree orphan cleanup (for debugging)
# DISABLE_WORKTREE_ORPHAN_CLEANUP=1

# Rust log level for development
# RUST_LOG=debug
